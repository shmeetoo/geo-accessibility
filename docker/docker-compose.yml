services:
  db:
    image: postgis/postgis:16-3.4
    container_name: geo_postgres
    environment:
      POSTGRES_DB: geo_db
      POSTGRES_USER: shmeetoo
      POSTGRES_PASSWORD: 1905
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: